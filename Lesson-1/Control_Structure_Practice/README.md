# Approach For Smallest Positive Integer
- Sort The Araay
`sort() method sorts the list ascending by default.`
- iterate the loop
- check if number is positive if yes return the number
`num>0` - positive
`num<0` - negative
